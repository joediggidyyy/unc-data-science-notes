# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/

# Jupyter
.ipynb_checkpoints/

# Temporary
*.tmp
*.bak

# Local scratch / extracted bundles
temp/

# Accidental nested repo copies
unc-data-science-notes/

# Local-only working area (planning drafts, snapshots, staging)
local_untracked/

# Agent/session artifacts (never commit)
.agent_session/
codesentinel.log

# Operational/runtime data (never commit)
logs/

# Reports are generated outputs (local-only; never publish)
reports/

# Semantics Vault local caches (keep the stable index files tracked; ignore snapshots/mirror caches)
semantics_vault/oracl_index/snapshots/
semantics_vault/oracl_index/mirrors/mirror_*/

# CLI-generated instruction backups (local provenance; do not publish by default)
quarantine_legacy_archive/backups/AGENT_INSTRUCTIONS_*_cli-*.md

# Python
__pycache__/
*.py[cod]
*$py.class

.pytest_cache/
.ruff_cache/

# Virtual environments
.venv/
.venv_*/
.venv-*/

# CodeSentinel (local-only; never publish)
.codesentinel/
codesentinel.log
codesentinel/
